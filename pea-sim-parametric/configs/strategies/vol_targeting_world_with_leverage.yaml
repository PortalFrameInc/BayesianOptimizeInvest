name: vol_targeting_world_with_leverage
target_weights:
  WORLD: 1.0
constraints:
  max_weight: 1.0
  allow_cash: true
overlays:
  vol_targeting:
    enabled: true
    target_vol_annual: 0.12
    lookback_days: 63
    max_leverage_multiplier: 1.0
    min_leverage_multiplier: 0.3
